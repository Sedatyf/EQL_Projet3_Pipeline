/**
 * This enum is part of the main. It is linked to the class ToolBox.
 */
package org.Shopizer;

/**
 * EBrowser is the enum of browser. It is called by the method chooseBrowser() on the class ToolBox.
 * @author formation
 *
 */
public enum EBrowser {
	f,e,c,ie;
}
